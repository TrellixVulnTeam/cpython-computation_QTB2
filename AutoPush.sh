git status

git add .

# echo 'Enter the commit message:'
#commitMessage = 'cpython and c compilation with python'

git commit -m "cpython and c compilation with python"

#echo 'Enter the name of the branch:'
#read branch

#git push origin $branch
git push origin

read